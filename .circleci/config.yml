jobs:
  do-nothing:
    docker:
      - image: bash:4.4.19
    resource_class: small
    steps:
      - run:
          name: Nothing to do
          command: "echo 'job done'"
workflows:
  version: 2
  build:
    jobs:
      - do-nothing:
          filters:
            branches:
              ignore: 
                - master
version: 2
